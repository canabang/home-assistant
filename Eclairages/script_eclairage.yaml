script:
  gerer_eclairage:
    alias: "Gérer Éclairage"
    description: "Script central pour gérer l'éclairage selon les conditions (présence, vocal, absence). Priorité vocale absolue. Transition douce après passage en mode jour."
    fields:
      piece:
        description: "Nom de la pièce (chambre, cuisine, salon, sam, sdb)"
        example: "chambre"
        required: true
        selector:
          text:
      source:
        description: "Source du déclenchement"
        example: "vocal_onoff"
        required: true
        selector:
          select:
            options:
              - "vocal_onoff"
              - "presence"
              - "absence"
      action:
        description: "Action spécifique (on, off, toggle). Doit être fourni pour vocal_onoff."
        example: "on"
        required: false
        selector:
          text:
    sequence:
      # ===== VÉRIFICATION PRÉALABLE : ÊTES-VOUS À LA MAISON ? =====
      - condition: template
        value_template: "{{ states('sensor.etat_canabang_et_device_tracker') == 'home' }}"

      # ===== INITIALISATION DES VARIABLES DE BASE =====
      - variables:
          light_entity: "light.hue_{{ piece }}"
          is_light_on: "{{ is_state('light.hue_' + piece, 'on') }}"
          mode_nuit: "{{ is_state('input_text.jour_nuit', 'nuit') }}"
          soleil_visible: "{{ state_attr('sun.sun', 'elevation') > 0 }}"

      # ===== DÉFINITION DE LA SCÈNE À ACTIVER (AVEC TRANSITION DOUCE DE 15 MIN) =====
          scene_a_activer: >
            {% set dernier_changement = states('input_datetime.dernier_changement_jour_nuit') | default('1970-01-01 00:00:00') %}
            {% set maintenant = now() %}
            {% set delta_minutes = (now() - datetime.strptime(dernier_changement, '%Y-%m-%d %H:%M:%S')).total_seconds() / 60 %}
            {% if mode_nuit %}
              scene.hue_{{ piece }}_1_veilleuse
            {% elif soleil_visible and delta_minutes < 15 %}
              scene.hue_{{ piece }}_1_veilleuse
            {% elif soleil_visible %}
              scene.hue_{{ piece }}_2_stimulation
            {% else %}
              scene.hue_{{ piece }}_3_attenue
            {% endif %}

      # ===== GESTION SELON LA SOURCE =====
      - choose:
          # ===== [1] COMMANDES VOCALES ON/OFF =====
          # Priorité ABSOLUE : ignore tous les blocages
          - conditions:
              - condition: template
                value_template: "{{ source == 'vocal_onoff' }}"
            sequence:
              - choose:
                  # ACTION OFF ou TOGGLE sur lumière allumée
                  - conditions:
                      - condition: or
                        conditions:
                          - condition: template
                            value_template: "{{ action == 'off' }}"
                          - condition: and
                            conditions:
                              - condition: template
                                value_template: "{{ action in ['toggle'] }}"
                              - condition: template
                                value_template: "{{ is_light_on }}"
                    sequence:
                      - service: light.turn_off
                        target:
                          entity_id: "{{ light_entity }}"

                  # ACTION ON ou TOGGLE sur lumière éteinte
                  - conditions:
                      - condition: or
                        conditions:
                          - condition: template
                            value_template: "{{ action == 'on' }}"
                          - condition: and
                            conditions:
                              - condition: template
                                value_template: "{{ action in ['toggle'] }}"
                              - condition: template
                                value_template: "{{ not is_light_on }}"
                    sequence:
                      - service: scene.turn_on
                        target:
                          entity_id: "{{ scene_a_activer }}"

          # ===== [2] DÉTECTION PRÉSENCE =====
          - conditions:
              - condition: template
                value_template: "{{ source == 'presence' }}"
            sequence:
              # 🔒 BLOCAGES : Ne pas activer si conditions spécifiques
              - choose:
                  # Chambre : bloqué si mode nuit OU lit occupé
                  - conditions:
                      - condition: template
                        value_template: "{{ piece == 'chambre' }}"
                      - condition: or
                        conditions:
                          - condition: template
                            value_template: "{{ mode_nuit }}"
                          - condition: state
                            entity_id: binary_sensor.esp_bed_occupation_master_bed_occupied
                            state: 'on'
                    sequence:
                      - stop: "Chambre : activation bloquée (mode nuit ou lit occupé)"

                  # SDB : bloqué si prismal ON OU luminosité > 2 lux
                  - conditions:
                      - condition: template
                        value_template: "{{ piece == 'sdb' }}"
                      - condition: or
                        conditions:
                          - condition: state
                            entity_id: switch.prismal
                            state: 'on'
                          - condition: numeric_state
                            entity_id: sensor.lux_sdb
                            above: 2
                    sequence:
                      - stop: "SDB : activation bloquée (prismal ON ou lux > 2)"

                  # Toutes pièces : bloqué si lumière déjà allumée
                  - conditions:
                      - condition: template
                        value_template: "{{ is_light_on }}"
                    sequence:
                      - stop: "Lumière déjà allumée : pas d'activation sur présence"

              # ✅ Si aucun blocage, on active la scène
              - service: scene.turn_on
                target:
                  entity_id: "{{ scene_a_activer }}"

          # ===== [3] DÉTECTION ABSENCE =====
          - conditions:
              - condition: template
                value_template: "{{ source == 'absence' }}"
            sequence:
              - service: light.turn_off
                target:
                  entity_id: "{{ light_entity }}"
