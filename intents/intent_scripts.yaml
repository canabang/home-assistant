# intent_scripts.yaml - Mapping direct pour intents spécifiques

# Café vocal
TriggerCoffeeAutomation:
  action:
    - service: automation.trigger
      target:
        entity_id: automation.cafe_vocal

# Routine matinale
TriggerWakeUpAutomation:
  action:
    - service: automation.trigger
      target:
        entity_id: automation.je_suis_reveille

# Mode dodo
TriggerBedtimeAutomation:
  action:
    - service: automation.trigger
      target:
        entity_id: automation.1742812962172
